# My Project

This is a sample project for testing the Aether intelligence pipeline.

## Getting Started

Run `npm install` then `npm start`.

## Architecture

This project uses Express.js for the backend API and React for the frontend.
Authentication is handled via JWT tokens stored in Redis sessions.
Payments are processed through Stripe.
